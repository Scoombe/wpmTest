language: node_js
before_install:
- npm install --only=dev
node_js:
- node
script:
  - npm run lint
  - npm run test
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN 
  keep-history: true
  on:
   branch: master
